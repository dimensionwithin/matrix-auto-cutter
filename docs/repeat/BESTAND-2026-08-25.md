# Bestand -- Shorts-Linie, Stand 2026-08-25

Fortschreibung von `BESTAND-2026-08-24.md`. Auftrag: uebergabe-25-08, rein
lesend ausser den beiden neuen Dokumenten unter `docs\repeat\`. Repository
`P:\DimensionWithin-MatrixMarketAutoEditor`.

**Wie dieses Dokument zu lesen ist:** Der Bestand vom 24.8. gilt weiter, wo
hier nichts anderes steht. Dieses Dokument nennt, was neu ist und was sich
geaendert hat, und sagt bei jedem uebernommenen Abschnitt ausdruecklich, dass
er unveraendert gilt — statt ihn wortreich zu wiederholen. Wer eine
Modulsignatur, eine Konstante oder ein Dateiformat der Stufen 0 bis 5d sucht,
schlaegt im Bestand vom 24.8. nach.

---

## 1. Stand

**HEAD:** `2855d5ffc988de11fec70f0f2cae0adbda01aac8` auf `master`, gepusht.

**`git status --porcelain` woertlich:**

```
?? -
```

Die Datei `-` (9 Byte, Inhalt `mp4-bytes`) ist ein Testrueckstand, Ursache
belegt in Abschnitt 12.1. Nicht committet, nicht geloescht.

**Die sechs Commits seit `f955792`** (`git log --oneline -8`, gekuerzt):

| Kuerzel | Betreff |
|---|---|
| `2855d5f` | Shorts: Arbeitskopie nimmt Seitendateien mit |
| `f5f7502` | Shorts: Auswahl aus Urteilen samt Trefferquote |
| `a83a497` | Shorts: Urteile und Kandidaten des Sonnet-Laufs vom 21.8. gesichert |
| `fd740cd` | Shorts: Auftragstext der Zerlegung als eigene Vorgabe |
| `8581b3e` | Shorts: Wortliste mit Interpunktion kopflos erzeugbar |
| `f3c87e3` | Doku: Orchestrator-Uebergabe und Bestand vom 24.8. |
| `f955792` | Shorts: verwaiste .bak-Staende als eigene Fassungen sichern |
| `ef014cf` | Shorts: Kriterien auf Fassung 0.8 gehoben |

Die aelteren Commits stehen im Bestand vom 24.8., Abschnitt 1.

**Umfang je Commit** (`git show --stat`):

| Commit | Dateien |
|---|---|
| `8581b3e` | `src/matrix_auto_cutter/shorts/wortliste.py` (+142), `tests/test_shorts_wortliste.py` (+112) |
| `fd740cd` | `docs/repeat/ZERLEGUNG-AUFTRAGSTEXT.md` (+169) |
| `a83a497` | `labels/repeat/kandidaten-2026-08-21-lauf1-sonnet.json` (+415), `labels/repeat/urteile-2026-08-21-lauf1-sonnet.json` (+254) |
| `f5f7502` | `labels/repeat/trefferquote.json` (+36), `src/matrix_auto_cutter/shorts/auswahl.py` (+460), `tests/test_shorts_auswahl.py` (+252) |
| `2855d5f` | `src/matrix_auto_cutter/shorts/build.py` (+9), `tests/test_shorts_build.py` (+94) |

---

## 2. Die Module der Shorts-Linie

**25 `.py`-Dateien** unter `src\matrix_auto_cutter\shorts\` (nachgezaehlt in
diesem Auftrag: `ls src/matrix_auto_cutter/shorts/*.py | wc -l` = 25). Der
Bestand vom 24.8. nennt 23; hinzugekommen sind `wortliste.py` (`8581b3e`) und
`auswahl.py` (`f5f7502`).

**Alle 23 Module des 24.8.-Bestands gelten unveraendert**, mit einer Ausnahme:
`build.py` ist um die Konstante `ARBEITSKOPIE_BEGLEITER_SUFFIXE` und vier
Zeilen in `_bereite_arbeitskopie_vor` gewachsen (Abschnitt 4). Alle uebrigen
Signaturen, Konstanten und CLI-Aufrufe stehen dort und wurden in dieser
Sitzung nicht angefasst.

Es folgen die beiden neuen Module vollstaendig.

---

## 3. `wortliste.py` (142 Zeilen) -- Stufe 2 Teil 1b: Wortliste kopflos

Zweck laut Modul-Docstring (Z.1-17): Die Zerlegung braucht eine Wortliste mit
Zeitmarken und Interpunktion. `words_from_whisper_json` (aus
`subtitle_lines.py`) baut sie bereits aus `transkript-rendered.wav.json` —
dieses Modul haengt nur Lesen, Schreiben und Wiederanlauf drumherum, ohne die
Funktion zu kopieren. Der Docstring begruendet ausdruecklich, warum ein
JSON-Parsefehler und eine leere Wortliste **laute** Fehlschlaege mit eigenem
Rueckgabecode sind: eine abgebrochene `*.wav.json` sieht von aussen fertig
aus, und ein stiller Rueckfall auf eine leere Liste wuerde die Zerlegung mit
leerer Eingabe weiterlaufen lassen.

**Importe** (Z.27-29), nichts kopiert:
`matrix_auto_cutter.atomic.replace_atomically`;
`shorts.subtitle_lines.Word`, `.words_from_whisper_json`;
`shorts.transcript.RENDERED_WAV_NAME`, `.transcript_paths`.

**Konstanten:**

| Name | Wert | Zeile |
|---|---|---|
| `WORTLISTE_FILE_NAME` | `"wortliste.json"` | 31 |
| `WORTLISTE_SCHEMA_VERSION` | `"1.0"` | 32 |
| `RUECKGABECODE_JOB_NICHT_LESBAR` | 2 | 34 |
| `RUECKGABECODE_ROHAUSGABE_FEHLT` | 2 | 35 |
| `RUECKGABECODE_ROHAUSGABE_KAPUTT` | 3 | 36 |
| `RUECKGABECODE_NULL_WOERTER` | 4 | 37 |

**Funktionen:**
- `wortliste_pfad(job_path: Path) -> Path` (Z.40) -- Aufnahmeordner
  (`job_path.parent`) plus Dateiname.
- `lade_rohausgabe(pfad: Path) -> str` (Z.45) -- liest als `utf-8`.
- `baue_wortliste_payload(woerter: Sequence[Word], *, quelle: str) -> dict[str, object]` (Z.50).
- `schreibe_wortliste(pfad: Path, payload: dict[str, object]) -> None` (Z.64)
  -- atomar, Muster aus `transcript.write_transcript`:
  `NamedTemporaryFile` im Zielordner, `flush`, `os.fsync`,
  `replace_atomically(..., create_only=False)`, Temp im `finally` entfernt.
- `main(argv: Sequence[str] | None = None) -> int` (Z.88).

**Rueckgabecodes von `main`:**

| Code | Fall | Meldung | Zeile |
|---|---|---|---|
| 2 | `job_path` ist keine Datei | `ANGEHALTEN [job_nicht_lesbar]: ...` | 104-106 |
| 0 | `wortliste.json` liegt vor, kein `--force` | `<Pfad> (<n> Woerter, wiederverwendet)` | 110-114 |
| 2 | Rohausgabe fehlt | `ANGEHALTEN [rohausgabe_fehlt]: ...` | 117-119 |
| 3 | Rohausgabe kein gueltiges JSON | `ANGEHALTEN [rohausgabe_kaputt]: ...` | 122-129 |
| 4 | null Woerter, **keine Datei geschrieben** | `ANGEHALTEN [null_woerter]: ...` | 131-133 |
| 0 | Erfolg | `<Pfad> (<n> Woerter, neu berechnet)` | 135-138 |

**CLI** (Z.96-101):

```powershell
python -m matrix_auto_cutter.shorts.wortliste JOB_PATH [--force]
```

Pflicht: `job_path` (Pfad zur `shorts-job.json`). `--force` ist `store_true`
und erzwingt Neuberechnung.

**Format `wortliste.json`** (`baue_wortliste_payload`, Z.52-61; JSON mit
`indent=2`, `sort_keys=True`, `ensure_ascii=False`, abschliessendem
Zeilenumbruch):

| Feld | Typ | Beleg-Wert (2026-08-21 10-46-08) |
|---|---|---|
| `artifact_type` | string, fest | `"matrix_auto_cutter_shorts_wortliste"` |
| `schema_version` | string, fest | `"1.0"` |
| `quelle` | string | `"transkript-rendered.wav.json"` (Dateiname der Rohausgabe) |
| `wort_anzahl` | int | `1569` |
| `woerter[].start_ms` / `.end_ms` / `.text` | int/int/string | erstes Wort `start_ms` 0, letztes Wort `end_ms` 584900 |

Probelauf-Belege aus `artefakte\repeat\shorts-wortliste\BERICHT-2026-08-24.md`,
TEIL 5: Datei 132.974 Byte, 1569 Woerter, Laufzeit 513 ms, zweiter Lauf
"wiederverwendet" mit byteweise identischem Aenderungszeitstempel.

**Eigene Testdatei:** `tests\test_shorts_wortliste.py`, 112 Zeilen, 6 Faelle
(Erfolg, Wiederverwendung ohne `--force`, `--force` rechnet neu, kaputte
Rohausgabe → Code 3, null Woerter → Code 4, fehlende Rohausgabe → Code 2).
Kein Aufruf von whisper/ffmpeg/ffprobe.

---

## 4. `auswahl.py` (460 Zeilen) -- zwischen Urteil und Bau

Zweck laut Modul-Docstring (Z.1-21): `build.py` liest ausschliesslich
`kandidaten.json` und kennt keine Urteile — ohne dieses Modul wuerde ein
Aufruf alle Kandidaten bauen, auch die verworfenen. `bauliste.json` traegt
bewusst dasselbe Schema wie `kandidaten.json` (Wurzel `kandidaten`,
unveraenderte `index`-Werte), damit sie `build.py` unveraendert als
`KANDIDATEN_PATH` uebergeben werden kann und die Ordnernamen
`kandidat-{index:02d}` auf die urspruenglichen Kandidaten bezogen bleiben.
`trefferquote.json` wird **angehaengt, nie ersetzt**.

**Importe** (Z.30-36): `matrix_auto_cutter.atomic.replace_atomically`;
`shorts.candidates.Candidate`, `.CandidatesSchemaError`, `.load_candidates`;
`shorts.judge_server.Urteil`, `.load_urteile`. Keine dieser Dateien wurde
geaendert.

**Konstanten:**

| Name | Wert | Zeile |
|---|---|---|
| `BAULISTE_FILE_NAME` | `"bauliste.json"` | 38 |
| `TREFFERQUOTE_PFAD` | `Path("labels/repeat/trefferquote.json")` -- **relativ**, s. Abschnitt 12.2 | 39 |
| `TREFFERQUOTE_SCHEMA_VERSION` | `"1.0"` | 40 |
| `_ZIELBEREICH_MIN_MS` | 8000 | 42 |
| `_ZIELBEREICH_MAX_MS` | 15000 | 43 |
| `_CODE_KANDIDATEN_UNLESBAR` | 2 | 45 |
| `_CODE_KEINE_URTEILSDATEI` | 2 | 46 |
| `_CODE_URTEILE_KEIN_JSON` | 3 | 47 |
| `_CODE_KEINE_ANNAHMEN` | 4 | 48 |
| `_CODE_URTEILE_ABWEICHUNG` | 5 | 49 |

**Funktionen:**
- `juengste_urteilsdatei(job_dir: Path) -> Path | None` (Z.52) -- dasselbe
  Suchmuster und Auswahlkriterium wie `judge_server.start_session_urteile`
  (`job_dir.glob("urteile*.json")`, Auswahl nach `st_mtime_ns`), nur lesend,
  ohne eine neue Sitzungsdatei anzulegen.
- `waehle_kandidaten(kandidaten, urteile) -> tuple[list[Candidate], list[Candidate], list[Candidate]]`
  (Z.66) -- `(angenommen, abgelehnt, ohne_urteil)`. **Angenommen heisst
  ausschliesslich `urteil == "ja"`.** `"nein"` und `"spaeter"` zaehlen als
  abgelehnt; kein Eintrag oder Eintrag mit `urteil is None` zaehlt als ohne
  Urteil.
- `pruefe_uebereinstimmung(kandidaten, urteile) -> list[str]` (Z.90) -- je
  Urteil: existiert der Index unter den Kandidaten, und stimmen `start_ms`,
  `end_ms` und `titel` ueberein. Die wichtigste Pruefung des Moduls.
- `_candidate_payload(candidate) -> dict[str, object]` (Z.127, intern) --
  genau die sieben Pflichtfelder des Kandidatenschemas.
- `baue_bauliste_payload(*, angenommen, abgelehnt_anzahl, ohne_urteil_anzahl, stammt_aus, urteile_aus) -> dict[str, object]` (Z.139).
- `_write_json_atomically(path, payload) -> None` (Z.164, intern) -- Muster
  aus `transcript.write_transcript`.
- `schreibe_bauliste(pfad, payload) -> None` (Z.188) -- ueberschreibt bei
  jedem Lauf.
- `_sicherheit_paar(kandidaten, angenommen_indizes)` (Z.193, intern) --
  `{stufe: {"ja": n, "nein": n}}` fuer `hoch`/`mittel`/`niedrig`.
- `_polarisierend_paar(kandidaten, angenommen_indizes, polarisierend_je_index)`
  (Z.205, intern) -- zaehlt **nur unter den Angenommenen**.
- `_lies_kandidaten_rohdaten(pfad) -> tuple[list[dict], dict[str, object]]`
  (Z.219, intern) -- liest `kandidaten.json` ein zweites Mal roh, weil
  `parse_candidates` sowohl Wurzelfelder (`modell`, `kriterien_fassung`,
  `lauf`, `video_name`) als auch Kandidaten-Zusatzfelder (`polarisierend`)
  wegschneidet. Rueckfaelle: `modell`/`kriterien_fassung`/`video_name` auf
  `"unbekannt"`, `lauf` auf `None` (die echte Datei traegt eine **Ganzzahl**,
  ein String-Rueckfall waere typisch falsch).
- `trefferquote_eintrag(*, video_name, lauf, modell, kriterien_fassung, kandidaten, angenommen, abgelehnt, ohne_urteil, polarisierend_je_index=None) -> dict[str, object]`
  (Z.255) -- reine Zaehlung, keine Wertung. `quote = round(len(angenommen) / gesamt, 3)`.
- `schreibe_trefferquote(pfad, eintrag) -> None` (Z.303) -- haengt an; passt
  ein vorhandener Eintrag auf `video_name` **und** `lauf`, passiert nichts.
- `_hat_bestehenden_eintrag(pfad, *, video_name, lauf) -> bool` (Z.336,
  intern) -- dieselbe Bedingung, vorab in `main`.
- `main(argv: list[str] | None = None) -> int` (Z.354).

**Rueckgabecodes von `main`:**

| Code | Fall | Meldung | Zeile |
|---|---|---|---|
| 2 | `kandidaten.json` unlesbar oder Schemafehler | `ANGEHALTEN [kandidaten_unlesbar]: ...` | 375-380 |
| 2 | keine `urteile*.json` gefunden bzw. `--urteile` fehlt | `ANGEHALTEN [keine_urteilsdatei]: ...` | 382-389 |
| 3 | Urteilsdatei kein gueltiges JSON | `ANGEHALTEN [urteile_kein_json]: ...` | 391-395 |
| 5 | Abweichung zwischen Urteil und Kandidat, **nichts geschrieben** | `ANGEHALTEN [urteile_abweichung]: ...` plus je Abweichung eine Zeile | 398-403 |
| 4 | null angenommene Kandidaten, **keine Datei** | `ANGEHALTEN [keine_annahmen]: ...` | 406-408 |
| 0 | Erfolg | `<n> von <m> angenommen, <a> abgelehnt, <o> ohne Urteil -> <Pfad>` | 450-454 |

Die Reihenfolge ist wichtig: `pruefe_uebereinstimmung` laeuft **vor**
`waehle_kandidaten` und vor jedem Schreibversuch.

**CLI** (Z.358-366):

```powershell
python -m matrix_auto_cutter.shorts.auswahl JOB_PATH [--kandidaten PFAD] [--urteile PFAD] [--output PFAD] [--keine-trefferquote]
```

Pflicht: `job_path` (der **Auftragsordner**, nicht die `shorts-job.json` --
`main` behandelt ihn als Verzeichnis, Z.368). Vorgaben:
`--kandidaten` = `job_path/kandidaten.json`, `--urteile` = juengste
`urteile*.json` im Auftragsordner, `--output` = `job_path/bauliste.json`,
`--keine-trefferquote` ist `store_true`.

**Format `bauliste.json`** (`baue_bauliste_payload`, Z.154-161):

| Feld | Typ | Beleg-Wert (Probelauf 25.8.) |
|---|---|---|
| `kandidaten` | Liste der Pflichtfelder je angenommenem Kandidaten (`index`, `start_ms`, `end_ms`, `titel`, `begruendung`, `sicherheit`, `enthaelt`) | 27 Eintraege, Indizes 1-31 ohne 2/6/24/28 |
| `stammt_aus` | string | Dateiname der Kandidatendatei |
| `urteile_aus` | string | Dateiname der Urteilsdatei |
| `angenommen` | int | 27 |
| `abgelehnt` | int | 4 |
| `ohne_urteil` | int | 0 |

Datei des Probelaufs: `artefakte\repeat\zerlegung-probelauf-1\urteil\bauliste.json`,
11.877 Byte. **Achtung:** `angenommen` traegt in von Hand abgeleiteten
Teillisten weiterhin die Zahl der Gesamtauswahl, s. Abschnitt 12.4.

**Format `labels\repeat\trefferquote.json`** (`schreibe_trefferquote`,
Z.329-332, plus `trefferquote_eintrag`, Z.284-300). Wurzel:

| Feld | Typ | Wert |
|---|---|---|
| `schema_version` | string, fest | `"1.0"` |
| `eintraege` | Liste | derzeit genau ein Eintrag |

Je Eintrag, mit dem in diesem Auftrag gelesenen Istwert der Datei:

| Feld | Typ | Wert |
|---|---|---|
| `video_name` | string | `"2026-08-21 10-46-08"` |
| `lauf` | int\|str\|null | `1` |
| `modell` | string | `"sonnet"` |
| `kriterien_fassung` | string | `"Fassung 0.8 (24. August 2026)"` |
| `kandidaten_gesamt` | int | `31` |
| `angenommen` / `abgelehnt` / `ohne_urteil` | int | `27` / `4` / `0` |
| `quote` | float, 3 Stellen | `0.871` |
| `sicherheit.<stufe>.{ja,nein}` | int | `hoch` 14/1, `mittel` 12/2, `niedrig` 1/1 |
| `polarisierend.{wahr,falsch}` | int | `12` / `15` (nur unter den Angenommenen) |
| `im_zielbereich_ja` / `im_zielbereich_nein` | int | `20` / `7` (Zielbereich 8.000-15.000 ms) |

**Eigene Testdatei:** `tests\test_shorts_auswahl.py`, 252 Zeilen, 10 Faelle.
`TREFFERQUOTE_PFAD` wird in den beiden Trefferquote-Tests per `monkeypatch`
auf `tmp_path` umgebogen, damit kein Testlauf die echte Datei beruehrt.

---

## 5. Die Aenderung in `build.py`

Commit `2855d5f`, neun Zeilen, eine Datei. Auftrag: `arbeitskopie-seitendateien`.

**Neue Konstante** neben `ARBEITSKOPIE_DIR_NAME`:

```python
ARBEITSKOPIE_BEGLEITER_SUFFIXE = (".json",)   # build.py:172
```

Docstring (Z.173-176): Endungen der Seitendateien, die mit jeder kopierten
Videodatei mitziehen. Gleicher Stamm im Quellordner, sonst uebersprungen (kein
Fehlschlag). **Nicht** `.mp4.framecount.json` — dafuer gilt die bestehende
Umleitung in `_framecount_sidecar_path`.

**Die Kopierschleife** in `_bereite_arbeitskopie_vor` (`build.py:1055-1058`)
kopiert nach jeder Videodatei zusaetzlich jede vorhandene Datei
`<stamm><suffix>` im selben Quellordner. Kein anderer Teil von `build.py`
wurde angefasst.

**Warum:** `avatar_canvas.read_avatar_coverage_missing_frames_front` liest die
Seitendatei `<avatar_path>.stem + ".json"` **neben dem ihr uebergebenen
Pfad** — und bekommt nach der Arbeitskopie den Pfad der Kopie. Vor dieser
Aenderung scheiterte deshalb jeder Bau mit `--output-dir` auf einem anderen
Laufwerk als die Quelle an `avatar_canvas_avatar_coverage_sidecar_missing`
(`artefakte\repeat\shorts-bau-probelauf\BERICHT-2026-08-25.md`, Abschnitt „Was
tatsaechlich schiefging").

**Befund der Voruntersuchung** (`arbeitskopie-seitendateien\BERICHT-2026-08-25.md`,
TEIL 1): `avatar_canvas.py:613` ist der **einzige** Fund im Modulverzeichnis,
der einen Seitendateinamen von einem uebergebenen Pfad ableitet. Alle anderen
`.parent / f"{stem}.json"`-Treffer (`build.py:1265`, `build.py:1724`,
`canvas.py:440`, `chart_crop.py:872`, `endcard.py:699`, `subtitle_burn.py:671`)
schreiben eigene Report-Ausgabedateien. Eine Seitendatei genuegt also.

Neben `avatar-cut.mp4` im Aufnahmeordner liegen genau zwei Begleiter:
`avatar-cut.json` (575 B, Stufe-1-Coverage) und
`avatar-cut.mp4.framecount.json` (217 B, eigene Umleitung, zieht **nicht**
mit).

**Drei neue Tests** in `tests\test_shorts_build.py` (+94 Zeilen):
`test_arbeitskopie_kopiert_seitendatei_mit`,
`test_arbeitskopie_ohne_seitendatei_kein_fehlschlag`,
`test_arbeitskopie_framecount_seitendatei_zieht_nicht_mit`. Reine
Dateisystem-Fixtures unter `tmp_path`, kein ffmpeg.

---

## 6. Die Stufen

Die Stufentabelle des Bestands vom 24.8. (Abschnitt 3) **gilt unveraendert**.
Zwei Ergaenzungen:

| Stufe | Datei/Funktion | Aufruf in `build.py` | Eingaben | Ausgaben |
|---|---|---|---|---|
| 2 Teil 1b (Wortliste) | `wortliste.py` (`main`) | **nicht aufgerufen** -- eigener CLI-Schritt nach der Transkription | `shorts-job.json`, `transkript-rendered.wav.json` | `wortliste.json` |
| 2 Teil 4 (Auswahl) | `auswahl.py` (`main`) | **nicht aufgerufen** -- eigener CLI-Schritt zwischen Urteil und Bau | `kandidaten.json`, juengste `urteile*.json` | `bauliste.json`, Eintrag in `labels\repeat\trefferquote.json` |

`build.py` bekommt seit dem 25.8. im Regelfall die `bauliste.json` als
`KANDIDATEN_PATH` uebergeben, nicht mehr die `kandidaten.json`.

---

## 7. Die Dateiformate

Abschnitt 4 des Bestands vom 24.8. **gilt unveraendert** fuer
`shorts-job.json`, `kandidaten.json`, `urteile-*.json`, `avatar-cut.json`,
`ausschnitt.json` (beide Formate), `shorts-bau-bericht.json`, `short.json`,
`transkript-rendered.json` und `transkript-rendered.wav.json`.

**Neu hinzugekommen:** `wortliste.json` (Abschnitt 3), `bauliste.json` und
`labels\repeat\trefferquote.json` (Abschnitt 4).

**Neu am Wurzelobjekt der Kandidatendatei**, seit
`docs\repeat\ZERLEGUNG-AUFTRAGSTEXT.md` (`fd740cd`): `kriterien_fassung`,
`achse` (immer `"gerendert"`), `video_name`, `video_dauer_ms`, `lauf`,
`modell`. Belegt am Istbestand von
`labels\repeat\kandidaten-2026-08-21-lauf1-sonnet.json`, in diesem Auftrag
gelesen:

```json
{
  "kriterien_fassung": "Fassung 0.8 (24. August 2026)",
  "achse": "gerendert",
  "video_name": "2026-08-21 10-46-08",
  "video_dauer_ms": 584900,
  "lauf": 1,
  "modell": "sonnet",
  "kandidaten": [ ... ]
}
```

`parse_candidates` liest ausschliesslich `payload["kandidaten"]` und laesst
alle Wurzelfelder liegen — sie koennen den Bau nicht brechen. **Wer nur
`load_candidates` benutzt, verliert sie stillschweigend**; `auswahl.py` ist
das einzige Modul der Kette, das die Datei deshalb zusaetzlich roh liest.

Ebenfalls neu je Kandidat, optional und vom Bau ignoriert:
`polarisierend`, `laengere_fassung`, `dauer_ms`, `erstes_wort`,
`letztes_wort`. `laengere_fassung` ist bisher von keinem Lauf befuellt worden.

**Der Zieldateiname der Zerlegung ist `kandidaten-lauf<N>.json`**, nicht
`kandidaten.json` — der Auftragstext verbietet Letzteres ausdruecklich, damit
ein zweiter Lauf den ersten nicht ueberschreibt.

---

## 8. Die Achsen und die Tonseite

Abschnitte 5 und 6 des Bestands vom 24.8. **gelten unveraendert**. In dieser
Sitzung wurde an `cursor_track.py`, `chart_crop.py`, `level_cut.py` und
`loop_point.py` keine Zeile geaendert.

Eine Beobachtung aus dem Bau vom 25.8., die dort einzuordnen ist: bei drei von
24 Kandidaten (21, 22, 30) griff der Stillevorlauf und schob die Startmarke
560-910 ms nach vorn; bei allen Kandidaten greifen `tonblende_grosszuegig`
oder `wortrand_kollision`. Die gebaute Dauer weicht dadurch um +377 bis
+440 ms vom rohen `end_ms - start_ms` ab, aber nur um bis zu 13 ms vom
angepassten Plan (`build_end_ms - build_start_ms`). Das ist gewolltes
Verhalten der Abschnitte 5/6, kein Befund.

---

## 9. Die CLI-Einstiege

Abschnitt 7 des Bestands vom 24.8. **gilt unveraendert** fuer die zehn dort
genannten Module. **Zwoelf** Module tragen jetzt ein `main(argv...)`; neu:

**Stufe 2 Teil 1b:**
```powershell
python -m matrix_auto_cutter.shorts.wortliste JOB_PATH [--force]
```
(`wortliste.py:96-101`; `job_path` Pflicht, `--force` = `store_true`)

**Stufe 2 Teil 4:**
```powershell
python -m matrix_auto_cutter.shorts.auswahl JOB_PATH [--kandidaten PFAD] [--urteile PFAD] [--output PFAD] [--keine-trefferquote]
```
(`auswahl.py:358-366`; `job_path` ist der Auftrags**ordner**)

Kein CLI, unveraendert: `candidates.py`, `judge.py`, `cursor_track.py`,
`avatar_axis.py`, `level_cut.py`, `loop_point.py`, `frame_map.py`,
`scene_windows.py`, `subtitle_lines.py`, `inventory.py`, `job.py`.

**Der volle Bauaufruf des 25.8., woertlich, aus der Repo-Wurzel:**

```powershell
uv run python -m matrix_auto_cutter.shorts.build "artefakte/repeat/shorts/2026-08-21 10-46-08/shorts-job.json" "artefakte/repeat/shorts-bau-vollstaendig/bauliste-rest.json" --output-dir "F:/MatrixMarketAutoEdit/Shorts-Rendered/2026-08-21 10-46-08"
```

---

## 10. Gates und Tests

Abschnitt 8 des Bestands vom 24.8. gilt der Form nach weiter; die Zahlen sind
gewachsen. Letzte belegte Gate-Laeufe dieser Sitzung
(`arbeitskopie-seitendateien\BERICHT-2026-08-25.md`, Abschnitt „Gates"):

| Gate | Ergebnis |
|---|---|
| `uv run python -m pytest` | **2605 passed, 1 skipped, 3 warnings** in 155,58 s |
| `uv run ruff check .` | **All checks passed!** |
| `uv run mypy src` | **genau 20 Fehler in 3 Dateien** -- `repeat/cut.py` (3), `repeat/cutcli.py` (7), `repeat/cli.py` (10) |

Wachstum der Suite: 2587 (21./24.8.) → 2592 (`8581b3e`, +6 Wortliste-Tests) →
2602 (`f5f7502`, +10 Auswahl-Tests) → 2605 (`2855d5f`, +3 Arbeitskopie-Tests).

**Die 20 mypy-Fehler sind vorbestehend und unveraendert** — dieselbe Zahl und
dieselbe Verteilung wie am 21.8. und 24.8. Weder `wortliste.py` noch
`auswahl.py` tragen einen eigenen Fehler.

**Die drei Warnungen** sind `PytestUnhandledThreadExceptionWarning` aus
ffprobe-Subprozess-Lesethreads (`UnicodeDecodeError` beim Dekodieren von
stderr unter `cp1252`) in `tests/phase2/test_findings_regressions.py` und
`tests/phase2/test_locks_native.py`. Vorbestehend, kein Fehlschlag.

**`uv run pytest` ohne `python -m` bricht ab** mit
`ModuleNotFoundError: No module named 'tests'` in `tests/repeat/` und
`tests/phase2/`, 20 Sammelfehler. Aufrufkonvention der bestehenden Suite.

**Shorts-Testdateien:** 22 (`tests\test_shorts_*.py`), neu
`test_shorts_wortliste.py` und `test_shorts_auswahl.py`.

---

## 11. Was nicht gebaut ist

Abschnitt 9 des Bestands vom 24.8. gilt weiter, mit drei Berichtigungen:

- **Die Kandidatensuche ist weiterhin kein Modul** — das bleibt Absicht. Sie
  hat aber seit `fd740cd` einen verbindlichen Auftragstext,
  `docs\repeat\ZERLEGUNG-AUFTRAGSTEXT.md`, 169 Zeilen, zwoelf Abschnitte:
  Rolle und Ergebnis, Platzhalter, Eingaben, Erste Handlung
  (Fassungspruefung, Halt unterhalb 0.8), Ausgabe Kandidatendatei, Ausgabe
  Begleitbericht, Selbstauskuenfte, Der Nachschlaglauf, Die zweite Haelfte,
  Was dieser Lauf NICHT tun darf, Wenn du nicht weiterkommst. Die Aussage des
  24.8.-Bestands, ein Auftragstext existiere nicht als Datei, ist damit
  ueberholt.
- **Die Luecke zwischen Urteil und Bau ist geschlossen** (`auswahl.py`).
- **Stufe 0 ist weiterhin nur ueber das Tk-Fenster bedienbar.** Ein kopfloser
  Weg fehlt; das Einmalskript vom 21.8. ist nicht auffindbar. Was
  `build_inventory` ohne Tk liefert und welche vier Dinge fehlen, steht in
  `kette-bestandsaufnahme\BERICHT-2026-08-24.md`, TEIL 2b.

**Weiterhin nicht gebaut:** Stufe 5d bleibt bewusst unverdrahtet; die
Zusammenfuehrung zweier Zerlegungslaeufe fehlt (weder Auftragstext noch Code);
ein Aufraeumer fuer die Zwischenstufen fehlt; eine Zustandsdatei je Aufnahme,
die den Fortschritt durch die Kette festhaelt, gibt es nicht (gesucht und
nicht gefunden); eine Fortschrittsanzeige waehrend der Transkription gibt es
nicht.

---

## 12. Die Umgebung und die Flecken

Abschnitt 10 des Bestands vom 24.8. (whisper-Binary, Modell, Vokabeldatei,
Ordner unter `F:`, Zustandsverzeichnis) **gilt unveraendert**. Zwei
Ergaenzungen:

- **Neuer Ausgabeort der Shorts:** `F:\MatrixMarketAutoEdit\Shorts-Rendered\<Aufnahme>\`,
  ein Ordner je Video. Am 25.8. angelegt fuer `2026-08-21 10-46-08`; daneben
  liegt `2026-08-21 10-46-08-fehlversuch\` (11.939.229 Byte, der umbenannte
  Zielordner des gescheiterten ersten Probelaufs, bewusst nicht geloescht).
  Freier Platz auf `F:` am 25.8.: 4970,28 GB von 5589,01 GB.
- **Speicherplatz auf `F:` nur mit `Get-Volume -DriveLetter F` messen.**
  `Get-PSDrive` meldete 0 GB frei, `fsutil` verweigerte den Zugriff, `wmic`
  war als Alias nicht vorhanden.

**Neue Dateien unter `labels\repeat\`** (Verzeichnisstand in diesem Auftrag
gelesen):

| Datei | Groesse | Herkunft |
|---|---|---|
| `kandidaten-2026-08-21-lauf1-sonnet.json` | 17.408 B | `a83a497` -- gesicherte Kandidatendatei des Sonnet-Laufs |
| `urteile-2026-08-21-lauf1-sonnet.json` | 6.057 B | `a83a497` -- die zugehoerigen Urteile |
| `trefferquote.json` | 711 B | `f5f7502` -- fortgeschrieben von `auswahl.py` |

Unveraendert daneben: `shorts-kriterien.yaml` (32.665 B, Fassung 0.8),
`shorts-kriterien-0.6.yaml.bak`, `urteile-2026-08-05.json`,
`whisper-vokabular.txt` (1.255 B) und zwei `.bak`-Staende des Vokabulars.

### 12.1 Die Datei `-` -- Ursache belegt

9 Byte, Inhalt `mp4-bytes`, im Repo-Wurzelverzeichnis. **Ursache gefunden**
(`zerlegung-auftragstext-nachtrag\BERICHT-2026-08-24.md`, TEIL 2):

`snap.py:55-65` baut den ffmpeg-Aufruf zur Stilleerkennung und endet auf
`"-f", "null", "-"`. `cutcli.py:43-50` importiert `detect_silence`, der
cutcli-Pfad loest den Aufruf also aus. Der Doppelgaenger `_FailOnSecondProbe`
in `tests\repeat\test_cutcli.py:385` faengt nur `powershell` und
`-show_entries` ab und behandelt alles uebrige als Encode: `Path(argv[-1])`
ist dann `-`, ein relativer Pfad, und `write_bytes(b"mp4-bytes")` schreibt
neun Byte ins Arbeitsverzeichnis. Der `_FakeRunner` derselben Datei hat bei
Zeile 70 genau den `silencedetect`-Zweig, der `_FailOnSecondProbe` fehlt.

**Nicht behoben** — `tests\repeat\` liegt im gesperrten Gebiet. Fuenf weitere
Doppelgaenger mit demselben `Path(argv[-1])`-Muster sind im Bericht gelistet,
aber nicht daraufhin geprueft, ob ihr Pfad `detect_silence` ueberhaupt
aufruft.

**`artefakte\` ist per `.gitignore:88` (`/artefakte/`) ausgeschlossen** — ein
Bericht dort kann in `git status` nie erscheinen.

### 12.2 `TREFFERQUOTE_PFAD` ist relativ

`Path("labels/repeat/trefferquote.json")` (`auswahl.py:39`) haengt am
Arbeitsverzeichnis des Aufrufers, wie alle anderen Shorts-Werkzeuge dieser
Kette. Im Probelauf war das die Repo-Wurzel. Fuer einen geplanten,
unbeaufsichtigten Lauf ist das eine Falle.

### 12.3 `build.py` prueft `READONLY_DRIVES` nicht gegen `output_dir`

`READONLY_DRIVES = frozenset({"F:"})` (`build.py:210`) hat genau eine
Lesestelle: `_framecount_sidecar_path` (`build.py:235`). Sie betrifft
ausschliesslich den Schreibort der Framezahl-Seitendatei. **Ein
`--output-dir F:\...` wird stillschweigend angenommen** — weder `main()` noch
`_bereite_arbeitskopie_vor` lehnen `F:` als Ziel ab
(`shorts-auswahl\BERICHT-2026-08-25.md`, TEIL 4). Seit der Entscheidung vom
25.8. ist das gewollt; wer die Konstante spaeter strenger auslegt, bricht den
Ausgabeweg.

### 12.4 `angenommen` in Teillisten

`bauliste-rest.json` traegt `angenommen: 27` bei 24 Eintraegen — bewusst so
belassen, derselben Konvention folgend wie `bauliste-probe.json`. `build.py`
liest das Feld nicht; ein Auswertungsskript wuerde darauf hereinfallen. Beide
Teillisten entstanden **von Hand**; eine Regel oder ein Werkzeug fuer
Teilbauten fehlt.

### 12.5 Widerspruch in den Punktdichte-Zahlen

Auf derselben `wortliste.json` mit denselben 1569 Woertern zaehlt
`shorts-wortliste\BERICHT-2026-08-24.md` **29 Woerter mit Punkt (1,8 %)** und
**424 mit Satzzeichen** (`.,!?;:`), waehrend
`zerlegung-probelauf-1\BERICHT-2026-08-25.md` **34 Satzende-Woerter (2,17 %)**
und **422 mit Satzzeichen** nennt. Beide beanspruchen exakte Messung. Die
Differenz 29/34 laesst sich durch die unterschiedliche Frage erklaeren (nur
`.` gegen `.`/`!`/`?`), die Differenz 424/422 nicht ohne Weiteres. **Beide
Fassungen stehen hier bewusst nebeneinander; wer die Zahlen braucht, zaehlt
einmal selbst nach.**

### 12.6 Uebernommen vom 24.8.

Die Flecken aus Abschnitt 11 des Bestands vom 24.8. gelten unveraendert
weiter: die lueckenhafte Fassungsgeschichte der Kriteriendatei (0.3 → 0.7
ohne Zwischenstaende, 0.6 nur als `.bak`); die verschwundene
+1-Frameabweichung (`ACHSENABWEICHUNG_MAX_FRAMES = 5`, Pruefung unveraendert
im Code, seit 21.8. durchgehend 0 — Ursache nicht belegt); der
Stillevorlauf-Rueckfall bei kandidat-08/-09 der Aufnahme vom 21.8.
(Verschiebungen −910 und −540 ms analysiert, nie gebaut); die vier Ordner mit
zusammengeschobenen Pfadnamen, deren Praemisse im Repo nicht belegbar war; der
fehlende turbo-Durchsatzwert in `UMGEBUNG.md`; die doppelten `MIN_PAUSE_MS`;
`ausschnitt.json`/`short.json` als Doppelnamen ohne eigenes Schema; und die
schnell veraltenden Zeilennummern in `level_cut.py`.

---

## Pruefsteine

- Jede Zahl, jeder Bezeichner und jede Zeilennummer in diesem Dokument stammt
  aus einer in diesem Auftrag gelesenen Datei oder aus einem der genannten
  Berichte. Zwei Aussagen sind ausdruecklich als **nicht belegt** bzw. als
  ungeklaerter Widerspruch gekennzeichnet: die Laufzeit der Zerlegung
  (Uebergabe, Abschnitt 3.1) und die Punktdichte-Zahlen (Abschnitt 12.5).
- `.py`-Dateien unter `src\matrix_auto_cutter\shorts\`: **25**, nachgezaehlt.
- Kein Code, kein Test, keine Kriteriendatei und kein bestehendes Dokument
  wurde in diesem Auftrag geaendert.
