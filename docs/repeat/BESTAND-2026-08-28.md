# Bestand — Shorts-Linie, Stand 2026-08-28

Fortschreibung von `BESTAND-2026-08-27-abend.md`. Auftrag: `uebergabe-28-08`,
rein lesend ausser den beiden neuen Dokumenten unter `docs\repeat\` und einer
Berichtigung in `docs\repeat\WECKER.md`.
Repository `P:\DimensionWithin-MatrixMarketAutoEditor`.

**Wie dieses Dokument zu lesen ist:** Der Bestand vom 27.8. abends gilt weiter,
wo hier nichts anderes steht — und er setzt seinerseits die Bestaende vom 27.8.
mittags, 25.8. abends und 25.8. nachmittags fort. Dieses Dokument nennt, was
seit `a3b9717` neu ist. Wer eine Modulsignatur, eine Konstante oder ein
Dateiformat sucht, das hier nicht steht, schlaegt dort nach.

Alle Zeilennummern sind in diesem Auftrag an den Dateien geprueft, Stand
`4eba561`.

---

## 1. Stand

**HEAD:** `4eba561` auf `master`, gepusht.

**`git status --porcelain` woertlich (vor dem Commit dieses Auftrags):**

```
?? -
```

Die Datei `-` (9 Byte) ist unveraendert der bekannte Testrueckstand, Ursache
belegt in Abschnitt 12.1 des Bestands vom 25.8. nachmittags. **Nicht anfassen,
nicht committen.**

**Die zwei Commits seit `0abc9b8`:**

| Kuerzel | Betreff | Dateien |
|---|---|---|
| `48858f0` | Shorts: Startskript und Anleitung fuer den naechtlichen Kettenlauf | `docs/repeat/WECKER.md` (234), `scripts/START-SHORTS-KETTE.ps1` (134), `tests/test_shorts_wecker.py` (142) |
| `4eba561` | Shorts: Aufraeumer entfernt Zwischenstufen im Bauordner | `src/matrix_auto_cutter/shorts/aufraeumen.py` (407), `tests/test_shorts_aufraeumen.py` (287) |

Die Zeilenzahlen in Klammern stammen aus `git show --stat` und sind im
Arbeitsbaum nachgezaehlt.

**`.py`-Dateien unter `src\matrix_auto_cutter\shorts\`: 29.** In diesem Auftrag
nachgezaehlt; `__pycache__` ist ein Ordner und zaehlt nicht mit. Es ist genau
eine Datei dazugekommen — `aufraeumen.py`. Die vollstaendige Liste:
`__init__.py`, `app.py`, **`aufraeumen.py`**, `auftrag.py`, `auswahl.py`,
`avatar_axis.py`, `avatar_canvas.py`, `avatar_cut.py`, `avatar_lag.py`,
`build.py`, `candidates.py`, `canvas.py`, `chart_crop.py`, `cursor_track.py`,
`endcard.py`, `frame_map.py`, `inventory.py`, `job.py`, `judge.py`,
`judge_server.py`, `kette.py`, `level_cut.py`, `loop_point.py`,
`scene_windows.py`, `subtitle_burn.py`, `subtitle_lines.py`, `transcript.py`,
`urteilslauf.py`, `wortliste.py`.

**Nicht angefasst und damit unveraendert seit `a3b9717`:** `auswahl.py` (1216
Zeilen), `urteilslauf.py` (878), `kette.py` (1065), `judge.py` (1544),
`judge_server.py`, `build.py`, `candidates.py`, alle Auftragstexte unter
`docs\repeat\`, `labels\repeat\trefferquote.json`. **Die Abschnitte 2 bis 11
des Bestands vom 27.8. abends gelten damit unveraendert fort**, mit der einen
Ausnahme in Abschnitt 6 dieses Dokuments (was nicht gebaut ist).

---

## 2. `scripts\START-SHORTS-KETTE.ps1` (134 Zeilen) — neu

**Widerspruch, ausdruecklich vermerkt.** `artefakte\repeat\wecker\BERICHT-2026-08-27.md`
TEIL 2 nennt die Datei mit **130 Zeilen**. `git show --stat 48858f0` und das
Nachzaehlen im Arbeitsbaum ergeben beide **134**. Welche Zahl der Bericht
meinte, ist **nicht belegt**; die 134 ist die nachpruefbare.

### 2.1 Parameter

```powershell
# START-SHORTS-KETTE.ps1:1-9
[CmdletBinding()]
param(
    [string]$Aufnahme,
    [switch]$Trocken,
    [string]$Modell
)
```

Alle drei **nur fuer den Handbetrieb**; sie reichen `--aufnahme`, `--trocken`
und `--modell` an `kette` durch (`:57-59`). Die Aufgabenplanung ruft das Skript
**ohne Parameter** auf — ohne `--aufnahme` waehlt die Kette die juengste
unverfallene Aufnahme selbst.

### 2.2 Die Repo-Wurzel steht fest

| Zeile | Inhalt |
|---|---|
| `:25` | `$RepoWurzel = 'P:\DimensionWithin-MatrixMarketAutoEditor'` |
| `:27-30` | fehlt sie: `Write-Host "ANGEHALTEN: Repo-Wurzel $RepoWurzel nicht erreichbar."`, `exit 1` |
| `:31` | `Set-Location -LiteralPath $RepoWurzel` |

Der Kommentar `:13-24` traegt die Begruendung: die Aufgabenplanung startet mit
**unbestimmtem Arbeitsverzeichnis** (in der Praxis meist `C:\Windows\system32`),
und `TREFFERQUOTE_PFAD` in `auswahl.py` ist **relativ**. Aus dem falschen
Verzeichnis heraus legt die Kette die Trefferquote an der falschen Stelle an,
**statt zu scheitern** — ein stiller Fehler.

`$RepoWurzel` ist die eine Stelle, die bei einem Umzug zu aendern ist. Der Test
`test_traegt_den_festen_repo_pfad` in `tests\test_shorts_wecker.py:39` haelt
beides zusammen.

### 2.3 Protokollort und -format

| Zeile | Inhalt |
|---|---|
| `:40` | `$ProtokollOrdner = Join-Path $RepoWurzel 'artefakte\repeat\kette-protokoll'` |
| `:41-43` | Ordner wird bei Bedarf angelegt (`New-Item -ItemType Directory -Force`) |
| `:44` | `$Protokoll = Join-Path $ProtokollOrdner ($Start.ToString('yyyy-MM-dd-HHmmss') + '.log')` |
| `:46-51` | `function Schreibe-Zeile` |

Eine Datei je Lauf, benannt `<jahr>-<monat>-<tag>-<stunde><minute><sekunde>.log`.
Jede Zeile:

```
[{0}] {1}  -f (Get-Date -Format 'yyyy-MM-dd HH:mm:ss'), $Text
```

also `[2026-08-28 14:58:26] Startskript in P:\DimensionWithin-MatrixMarketAutoEditor`.
Geschrieben wird mit `Add-Content -Encoding utf8` und **gleichzeitig** ueber
`Write-Host` auf stdout.

`kette.py` schreibt **selbst kein Protokoll** — kein `logging`, kein
`FileHandler`. Die gesamte Fortschrittsausgabe laeuft ueber `print(...)`. Das
Umlenken ist vollstaendig Sache dieses Skripts.

**Die Schlusszeile** (`:123-129`):

```
ZUSAMMENFASSUNG Start {0} Ende {1} Dauer {2} Rueckgabecode {3} (gemeldet {4}) Aufnahme {5}
```

`Rueckgabecode` ist der der Kette, `gemeldet` der des Skripts. Sie gehen **nur**
im Fall „nichts zu tun" auseinander (2 gegen 0). `Dauer` im Format `hh\:mm\:ss`.

### 2.4 Die Behandlung von Code 2

`:111-119`:

```powershell
$Gemeldet = $Rueckgabecode
if ($Rueckgabecode -eq 2) {
    if ($KeineArbeit) {
        Schreibe-Zeile 'Nichts zu tun: keine unverfallene Aufnahme vorhanden. ...'
    } else {
        Schreibe-Zeile 'Rueckgabecode 2 ohne den erwarteten Marker ... -- trotzdem als "nichts zu tun" gewertet, aber nachsehen lohnt.'
    }
    $Gemeldet = 0
}
```

**Code 2 wird auf 0 gesenkt, jeder andere von null verschiedene Code
unveraendert weitergereicht** (`exit $Gemeldet`, `:134`).

Die Begruendung im Kommentar `:95-110` ist genau: `CODE_KEINE_AUFNAHME = 2`
wird in `kette.py` an **genau zwei Stellen** vergeben, beide in
`bestimme_aufnahme` — gar keine Aufnahme im Bestand und nur eine verfallene.
Echte Fehlschlaege tragen 5, 9 oder 10.

`$KeineArbeit` wird beim Durchreichen der Ausgabe gesetzt (`:89`):

```powershell
if ($text -match 'ANGEHALTEN \[(keine_aufnahme|nur_verfallen)\]') { $KeineArbeit = $true }
```

Der Marker ist eine **zweite, unabhaengige Probe**: bleibt er aus, waehrend der
Code 2 lautet, wird der Lauf trotzdem als „nichts zu tun" gewertet — aber mit
einer eigenen Protokollzeile, die zum Nachsehen auffordert. Faellt `kette.py`
je auseinander, sieht man es hier, statt es zu verschlucken.

**Ebenso mitgelesen wird die gewaehlte Aufnahme** (`:88`):

```powershell
if ($text -match '^\s*Aufnahme:\s+(.+?)\s*$') { $GewaehlteAufnahme = $Matches[1] }
```

`(.+?)` statt `(\S+)`: Aufnahmenamen enthalten ein Leerzeichen
(`2026-08-25 15-14-00`), `\S+` schnitt sie hinter dem Datum ab. Der erste
Trockenlauf vom 28.8. hat das gezeigt. Bestimmt wird die Aufnahme **nicht ein
zweites Mal eigenstaendig** — zwei Bestimmungswege koennten auseinanderlaufen.

### 2.5 `ErrorActionPreference` — zweimal gesetzt, mit Absicht

| Zeile | Wert | Warum |
|---|---|---|
| `:11` | `'Stop'` | Voreinstellung des Skripts: jeder Fehler haelt an |
| `:81-82` | auf `'Continue'` **gesenkt**, voriger Wert in `$VorigeFehlerart` gemerkt | siehe unten |
| `:93` | zurueck auf `$VorigeFehlerart` | direkt nach dem Lauf |

Die Begruendung im Kommentar `:77-80`: in **Windows PowerShell 5.1** verpackt
`2>&1` jede stderr-Zeile eines fremden Programms in einen `ErrorRecord`. Bei
`'Stop'` haette schon eine harmlose Warnung von `uv` oder `ffmpeg` das Skript
abgebrochen, **bevor die Kette fertig ist**.

Der Lauf selbst, `:84-92`:

```powershell
& uv @Argumente 2>&1 | ForEach-Object { ... Schreibe-Zeile $text }
$Rueckgabecode = $LASTEXITCODE
```

### 2.6 Was das Skript nicht tut

Es ruft **weder `urteilslauf` noch einen Bau noch `git`**. Der einzige Treffer
auf diese Woerter ist die Schlussbemerkung `:131-133`, die gerade festhaelt,
dass es sie nicht ruft. Der Test `test_kein_urteil_kein_bau_kein_git`
(`tests\test_shorts_wecker.py:76`) prueft auf einen **Aufruf**, nicht auf das
blosse Wort.

### 2.7 Tests

`tests\test_shorts_wecker.py`, 142 Zeilen, **11 Testfunktionen, 12 Faelle**
(`test_handschalter_werden_durchgereicht` ist mit zwei Paaren parametrisiert).
Geprueft wird der **Text** der Datei — ein PowerShell-Skript laesst sich aus
pytest nicht sinnvoll ausfuehren, der Lauf dauerte anderthalb Stunden und riefe
ein Modell an.

Zwei Tests gehen ueber Textgleichheit hinaus, und sie sind die wichtigen:

- `test_nichts_zu_tun_code_stimmt_mit_kette_ueberein` (`:107`) — die 2 im
  Skript ist `kette.CODE_KEINE_AUFNAHME`, **importiert und verglichen**, keine
  geratene Zahl.
- `test_nichts_zu_tun_bleibt_von_fehlschlaegen_unterscheidbar` (`:113`) — liest
  **alle** `CODE_*`-Zuweisungen aus `kette.py` und stellt sicher, dass keine
  zweite die 2 traegt.

Begruendung: das Skript senkt Code 2 auf 0. Bekaeme ein echter Fehlschlag je
dieselbe 2, meldete das Skript der Aufgabenplanung **stillschweigend Erfolg**,
wo ein Fehlschlag vorliegt — der schlimmste Ausgang, weil niemand ihn bemerkt.

---

## 3. `src\matrix_auto_cutter\shorts\aufraeumen.py` (407 Zeilen) — neu

Einziger Import aus der Linie: `RENDER_WURZEL` aus `urteilslauf` (`:29`), also
`F:\MatrixMarketAutoEdit\Shorts Rendered`. Dazu `replace_atomically` aus
`matrix_auto_cutter.atomic` (`:28`).

### 3.1 Konstanten

| Zeile | Name | Wert |
|---|---|---|
| `:31` | `ZWISCHENSTUFEN` | `("ausschnitt.mp4", "leinwand.mp4", "mit-avatar.mp4")` |
| `:40` | `SHORT_NAME` | `"short.mp4"` |
| `:41` | `PROTOKOLL_DIR` | `Path("artefakte/repeat/aufraeumen")` |
| `:42` | `MINDESTALTER_STUNDEN_VORGABE` | `48` |
| `:52` | `CODE_ERFOLG` | `0` |
| `:53` | `CODE_WURZEL_FEHLT` | `2` |
| `:54` | `CODE_KEINE_AUFNAHME` | `3` |
| `:55` | `CODE_ORDNER_OHNE_SHORT` | `4` |
| `:57` | `_CODE_NAMEN` | `{2: "wurzel_fehlt", 3: "keine_aufnahme", 4: "ordner_ohne_short"}` |
| `:63` | `_STUNDE_SEKUNDEN` | `3600.0` |

**`ZWISCHENSTUFEN` ist eine FESTE Liste, kein Muster.** Der Docstring `:32-38`
woertlich: „Absichtlich kein ``glob("*.mp4")`` und keine Endungsregel: eine
Musterregel loescht eines Tages etwas, das noch nicht erfunden war. Kommt eine
fuenfte Stufe hinzu, wird sie hier von Hand eingetragen — oder sie bleibt
liegen, was der harmlose der beiden Fehler ist."

**`MINDESTALTER_STUNDEN_VORGABE`** meint die Aenderungszeit der **juengsten
`short.mp4`** einer Aufnahme, nicht die Aufnahmezeit im Ordnernamen — anders
als `urteilslauf.VERFALL_STUNDEN`, das denselben Wert 48 traegt. Docstring
`:43-50`: solange eine Aufnahme noch im Umlauf ist, koennte ein Teilbau folgen;
„der Nutzen des Loeschens ist in diesem Fenster gering und das Risiko
unnoetig."

### 3.2 Die drei Datenklassen

```python
# aufraeumen.py:66
@dataclass(frozen=True)
class KandidatPlan:
    ordner: Path
    dateien: tuple[Path, ...]
    bytes_frei: int
    grund: str

# aufraeumen.py:76
@dataclass(frozen=True)
class Plan:
    aufnahme_dir: Path
    loeschbar: tuple[KandidatPlan, ...] = ()
    uebersprungen: tuple[KandidatPlan, ...] = ()

# aufraeumen.py:100
@dataclass
class Ergebnis:
    geloescht: list[dict[str, object]] = field(default_factory=list)
    verschwunden: list[str] = field(default_factory=list)
    uebersprungen: list[dict[str, str]] = field(default_factory=list)
    bytes_frei: int = 0
```

`Plan` traegt drei berechnete Eigenschaften: `dateien` (`:84`, jede loeschbare
Datei ueber alle Ordner), `bytes_frei` (`:89`, die Summe) und `ohne_short`
(`:94`, die uebersprungenen Ordner, deren Grund mit `"keine "` beginnt).

**`Plan` ist frozen und vollstaendig, bevor etwas geschieht.** Erst planen,
dann ausfuehren — das ist die Trennung, die den Trockenlauf ueberhaupt
ermoeglicht.

### 3.3 `darf_aufraeumen`

```python
# aufraeumen.py:133
def darf_aufraeumen(kandidat_dir: Path) -> tuple[bool, str]: ...
```

**Wahr NUR, wenn eine `short.mp4` im selben Ordner liegt und groesser als
0 Byte ist.** Drei Rueckgaben:

| Fall | Rueckgabe |
|---|---|
| keine Datei | `(False, "keine short.mp4")` |
| Groesse ≤ 0 | `(False, "short.mp4 ist leer (0 Byte)")` |
| sonst | `(True, "short.mp4 vorhanden (<n> Byte)")` |

Docstring: die `short.mp4` ist das Ergebnis der ganzen Linie; „fehlt sie oder
ist sie leer, ist der Bau unfertig und die Zwischenstufen sind das einzige, was
von ihm uebrig ist."

Der Grundtext wandert unveraendert in das Protokoll und in die
`ANGEHALTEN`-Meldung — der Praefix `"keine "` ist zugleich die Erkennungsmarke
fuer `Plan.ohne_short`.

### 3.4 `_ist_schlichter_name`

```python
# aufraeumen.py:278
def _ist_schlichter_name(name: str) -> bool: ...
```

Wahr nur fuer einen **blossen Ordnernamen**. Abgewiesen wird:

| Zeile | Bedingung |
|---|---|
| `:286` | leerer Name, `"."`, `".."` |
| `:288` | `Path(name).is_absolute()` oder `len(Path(name).parts) != 1` |
| `:290` | enthaelt `/`, `\` oder `:` |

Docstring: „`--aufnahme` nimmt einen NAMEN, keinen Pfad. Ohne diese Pruefung
traegt `wurzel / name` ein `..` oder einen absoluten Pfad klaglos aus der
Renderwurzel heraus, und ein Werkzeug, das loescht, darf sich nicht darauf
verlassen, dass der Aufrufer es gut meint."

Geprueft ueber `tests\test_shorts_aufraeumen.py:273-276` mit sechs Faellen:
`".."`, `"../Rendered"`, `r"..\Rendered"`, `r"C:\Windows"`, `"unter/ordner"`,
`""`.

### 3.5 `plane_aufraeumung`

```python
# aufraeumen.py:158
def plane_aufraeumung(aufnahme_dir: Path) -> Plan: ...
```

Je Kandidatenordner (`_kandidatenordner`, `:150`, sortiert, Name beginnt mit
`"kandidat-"`):

1. `darf_aufraeumen` — nein → `uebersprungen` mit Grund;
2. `zwischenstufen(ordner)` (`:121`, nur exakte Namen aus `ZWISCHENSTUFEN`, in
   fester Reihenfolge) — leer → `uebersprungen` mit
   `"keine Zwischenstufe mehr vorhanden"`;
3. sonst Summe der Groessen bilden und in `loeschbar` eintragen.

Ein nicht vorhandener `aufnahme_dir` ergibt einen **leeren Plan** (`:160-161`),
keine Ausnahme.

### 3.6 `fuehre_aus`

```python
# aufraeumen.py:203
def fuehre_aus(plan: Plan) -> Ergebnis: ...
```

Die uebersprungenen Ordner wandern zuerst unveraendert ins Ergebnis
(`:210-213`). Dann wird geloescht — **`pfad.unlink()` in `:218` ist der einzige
Loeschaufruf des Moduls**, und seine einzige Quelle ist `ZWISCHENSTUFEN`.

Zwei Ausnahmen werden gefangen, keine bricht den Lauf ab:

| Ausnahme | Folge |
|---|---|
| `FileNotFoundError` | `verschwunden zwischen Plan und Ausfuehrung: <pfad>`, Eintrag in `ergebnis.verschwunden` |
| `OSError` | `nicht loeschbar: <pfad> (<fehler>)`, Eintrag in `ergebnis.uebersprungen` |

Docstring: „Eine Datei, die zwischen Plan und Ausfuehrung verschwindet, ist kein
Fehlschlag: sie wird gemeldet, und der Lauf geht weiter."

**Es gibt kein `rmdir` und kein `rmtree` im Modul.** Der Kandidatenordner
bleibt auch dann stehen, wenn er leer wird.

### 3.7 Weitere Funktionen

```python
# aufraeumen.py:110
def finde_bauordner(wurzel: Path) -> list[Path]: ...
# aufraeumen.py:121
def zwischenstufen(kandidat_dir: Path) -> list[Path]: ...
# aufraeumen.py:150
def _kandidatenordner(aufnahme_dir: Path) -> list[Path]: ...
# aufraeumen.py:184
def juengste_short_zeit(aufnahme_dir: Path) -> float | None: ...
# aufraeumen.py:194
def alter_stunden(aufnahme_dir: Path, jetzt: float | None = None) -> float | None: ...
# aufraeumen.py:234
def _protokoll_pfad(jetzt: datetime | None = None) -> Path: ...
# aufraeumen.py:239
def schreibe_protokoll(pfad: Path, aufnahmen: list[str], ergebnis: Ergebnis, wirklich: bool) -> Path: ...
# aufraeumen.py:262
def _melde_angehalten(code: int, text: str) -> None: ...
# aufraeumen.py:266
def _zeige_plan(plan: Plan) -> None: ...
# aufraeumen.py:293
def _waehle_aufnahmen(wurzel: Path, aufnahme: str | None, alle: bool, mindestalter: float) -> tuple[list[Path], list[str]]: ...
# aufraeumen.py:321
def main(argv: list[str] | None = None) -> int: ...
```

**Achtung, Namensgleichheit:** `aufraeumen.alter_stunden` nimmt einen **Pfad**
und misst die Aenderungszeit der juengsten `short.mp4`;
`urteilslauf.alter_stunden` nimmt einen **Namen** und misst die Aufnahmezeit im
Ordnernamen. Zwei verschiedene Funktionen mit demselben Namen und derselben
Schwelle 48 — beim Lesen nicht verwechseln.

**`_waehle_aufnahmen` ist die Stelle, an der das Mindestalter greift** — und
nur dort:

- **mit `--aufnahme`** (`:297-301`): `_ist_schlichter_name` pruefen, dann
  `wurzel / aufnahme`, falls es ein Ordner ist. **Kein Mindestalter, keine
  Hinweise.**
- **ohne `--aufnahme`** (`:302-318`): ueber `finde_bauordner` laufen, je Ordner
  `alter_stunden`; `None` → Hinweis `"<name>: keine short.mp4 gefunden"`;
  unter der Schwelle → Hinweis `"<name>: erst N h alt (Mindestalter 48 h)"`;
  sonst `reif`. **Ohne `--alle` wird nur die erste reife Aufnahme genommen**
  (`reif[:1]`).

### 3.8 Das Protokollschema

`schreibe_protokoll` (`:239`) schreibt **atomar** — erst nach
`<name>.json.tmp`, dann `replace_atomically`. JSON mit `ensure_ascii=False,
indent=2`, Kodierung `utf-8`. Der Pfad kommt aus `_protokoll_pfad`:
`artefakte/repeat/aufraeumen/%Y-%m-%d-%H%M%S.json`.

Acht Wurzelfelder, `:244-253`:

| Feld | Typ | Inhalt |
|---|---|---|
| `zeitpunkt` | Zeichenkette | `datetime.now().isoformat(timespec="seconds")` |
| `wirklich_geloescht` | Wahrheitswert | ob `--wirklich-loeschen` gesetzt war |
| `aufnahmen` | Liste von Zeichenketten | die Ordnernamen der geraeumten Aufnahmen |
| `geloescht` | Liste von Objekten | je `{"pfad": <str>, "bytes": <int>}` |
| `bytes_frei` | Ganzzahl | Summe der freigegebenen Bytes |
| `anzahl_geloescht` | Ganzzahl | `len(geloescht)` |
| `verschwunden` | Liste von Zeichenketten | Pfade, die zwischen Plan und Ausfuehrung verschwanden |
| `uebersprungen` | Liste von Objekten | je `{"ordner": <str>, "grund": <str>}` |

**Das Protokoll wird auch bei einem Abbruch geschrieben:** die Loeschschleife
steht in einem `try` (`:381-387`), das Schreiben in dessen `finally`
(`:388-391`). Kommentar `:379-380`: „geloeschte Dateien ohne Nachweis waeren
der schlimmere Ausgang."

### 3.9 CLI und Rueckgabecodes

```
python -m matrix_auto_cutter.shorts.aufraeumen
       [--aufnahme NAME] [--alle] [--wirklich-loeschen]
       [--mindestalter-stunden ZAHL]
```

| Zeile | Fahne | Wirkung |
|---|---|---|
| `:327` | `--aufnahme NAME` | genau diese Aufnahme; **umgeht das Mindestalter**; nur ein schlichter Name |
| `:328` | `--alle` | alle reifen Aufnahmen statt nur der ersten |
| `:329` | `--wirklich-loeschen` | ohne sie wird **nichts** geloescht |
| `:330-332` | `--mindestalter-stunden ZAHL` | Vorgabe `48` |

Der Ablauf von `main` (`:321-403`):

1. Renderwurzel pruefen → sonst `CODE_WURZEL_FEHLT` (2).
2. `_waehle_aufnahmen`, Hinweise als `  uebergangen: <text>` ausgeben.
3. Keine Aufnahme → `CODE_KEINE_AUFNAHME` (3), mit zwei verschiedenen Texten je
   nachdem, ob `--aufnahme` gesetzt war.
4. Plaene bilden und zeigen (`_zeige_plan`), dann
   `Summe: <n> Dateien, <m> Byte`.
5. Ohne `--wirklich-loeschen`: `Nur Plan - ohne --wirklich-loeschen wird nichts
   geloescht.` — und dennoch `CODE_ORDNER_OHNE_SHORT` (4), falls es Ordner ohne
   `short.mp4` gibt; sonst `CODE_ERFOLG` (0).
6. Sonst loeschen, Protokoll schreiben,
   `Geloescht: <n> Dateien, <m> Byte frei. Protokoll: <pfad>` ausgeben; wieder
   4, falls Ordner ohne `short.mp4` dabei waren, sonst 0.

| Code | Name | Bedeutung |
|---:|---|---|
| 0 | — | Erfolg, auch wenn nichts zu tun war |
| 2 | `wurzel_fehlt` | Renderwurzel nicht erreichbar |
| 3 | `keine_aufnahme` | keine erfuellt das Mindestalter, oder die benannte fehlt |
| 4 | `ordner_ohne_short` | gemeldet, **nicht angefasst**; die uebrigen werden trotzdem aufgeraeumt |

Meldeformat `ANGEHALTEN [<code_name>]: <text>` (`:262-263`).

**Code 4 ist kein Abbruch.** Er wird erst am Ende vergeben, nachdem alles
Uebrige geraeumt wurde — ein unfertiger Bauordner soll auffallen, aber nicht
die Arbeit an den anderen 43 verhindern.

### 3.10 Tests

`tests\test_shorts_aufraeumen.py`, 287 Zeilen, **14 Testfunktionen, 19 Faelle**
(`test_aufnahme_muss_ein_schlichter_name_sein` ist mit sechs Namen
parametrisiert). Kein Test schreibt auf `F:`.

---

## 4. `docs\repeat\WECKER.md` — Abschnitt 2 berichtigt

Die Datei kam mit `48858f0` neu ins Repository (234 Zeilen) und traegt nach
dieser Berichtigung **293 Zeilen**. Dieser Auftrag hat **nur Abschnitt 2**
ergaenzt; die Abschnitte 1 und 3 bis 6 sind unveraendert. Abschnitt 2 hat zwei
neue Unterueberschriften bekommen — „Die Anmeldeart: es bleibt bei ‚Nur
interaktiv'" (`WECKER.md:119`) und „Die Energieverwaltung muss aus dem Weg"
(`:136`).

Was dazugekommen ist:

1. Der bisherige `schtasks`-Aufruf bleibt woertlich stehen, jetzt
   ausdruecklich gekennzeichnet als **Fassung fuer die Eingabeaufforderung**
   (`cmd.exe`).
2. Daneben die **PowerShell-taugliche Fassung** mit `$aufruf` und
   `schtasks.exe @(...)`, plus der Hinweis auf `cmd /c` als dritten Weg.
3. Ein Satz zur **Ursache**: PowerShell zerlegt die maskierten
   Anfuehrungszeichen im Wert von `/TR` anders, `schtasks` meldet dann
   `Ungueltige Syntax. Erforderliche Option sc fehlt.`
4. Die Erfahrung zur **Passwortabfrage**: „Unabhaengig von der
   Benutzeranmeldung ausfuehren" verlangt das Windows-Passwort, das bei einem
   Microsoft-Konto nicht die PIN ist; es bleibt bei „Nur interaktiv", und das
   genuegt — gesperrt ja, abgemeldet nein.
5. Die vier **Energieeinstellungen**, die per PowerShell gesetzt wurden.

Die Schaltertabelle, die Weck-Anleitung ueber `taskschd.msc` und alles Uebrige
sind unangetastet.

---

## 5. Was aus dem Bestand vom 27.8. abends unveraendert weiter gilt

**Ausdruecklich, damit niemand nachschlagen muss, ob sich etwas verschoben
hat:**

| Abschnitt dort | Inhalt | Gilt |
|---|---|---|
| 2 | Die Module der Shorts-Linie | **unveraendert**, plus `aufraeumen.py` |
| 3 | `auswahl.py` (1216 Zeilen) — Vorauswahl, Kennungen, Rueckfall | **unveraendert** |
| 4 | `urteilslauf.py` (878 Zeilen) — Verfall und Teilbau | **unveraendert** |
| 5 | `kette.py` (1065 Zeilen) — Verfall in `bestimme_aufnahme` | **unveraendert** |
| 6 | `judge.py` (1544 Zeilen) — Vorauswahl auf der Urteilsseite | **unveraendert** |
| 7 | Schema von `buendel.json` | **unveraendert** |
| 8 | `labels\repeat\trefferquote.json`, drei Eintraege, SHA-256 der Arbeitsbaumfassung `9b2c60d1545297e0a6aafb37e0f4d4306c5d4af0779579a1b816f87e12250223` | **unveraendert**; in diesem Auftrag nicht neu gehasht |
| 9 | Auftragstexte `BUENDELUNG-AUFTRAGSTEXT.md`, `ZERLEGUNG-AUFTRAGSTEXT.md` | **unveraendert** |
| 10 | Gates und Tests | siehe Abschnitt 6 unten |

Ebenso gilt weiter: `VERFALL_STUNDEN = 48` in `urteilslauf.py:71`,
`VORAUSWAHL_GROESSE = 15` in `auswahl.py:49`, `TEILBAULISTE_FILE_NAME =
"bauliste-offen.json"` in `urteilslauf.py:91`, `RENDER_WURZEL =
r"F:\MatrixMarketAutoEdit\Shorts Rendered"` in `urteilslauf.py:63` — alle in
diesem Auftrag nachgesehen.

---

## 6. Gates

Beide Auftraege des 28.8. haben ihre Gates gefahren. Werte aus den Berichten
uebernommen — **in diesem Auftrag NICHT nachgefahren**, pytest, ruff und mypy
sind hier gesperrt.

| Gate | `wecker` | `aufraeumen` |
|---|---|---|
| `uv run python -m pytest` | 2812 bestanden, 1 uebersprungen, 3 Warnungen, 0 Fehler, 168 s | 2825 bestanden, 1 uebersprungen, 0 Fehler |
| `uv run ruff check .` | All checks passed! | All checks passed! |
| `uv run mypy src` | 20 Fehler in 3 Dateien (130 Quelldateien geprueft) | 20 Fehler in 3 Dateien, keiner im neuen Modul |

Der Auftrag dieses Dokuments hat **keine Zeile Code geaendert**; Gates
entfielen dementsprechend.

**Anmerkung zur Testzahl.** 2800 (27.8. abends) → 2812 (nach `wecker`, +12) →
2825 (nach `aufraeumen`, +13). Der Aufraeumbericht nennt „19 neue Tests"; die
Differenz 2825 − 2812 = 13 passt nicht dazu. **Beide Zahlen stehen hier.** Eine
Erklaerung, die zu beiden passt: der Bericht nennt die 2825 ausdruecklich als
Stand **„davor"** und ergaenzt, die 19 neuen bestuenden ebenfalls — dann waere
der Endstand 2844 und nirgends notiert. Welche Lesart stimmt, ist **nicht
belegt**; pytest war in diesem Auftrag gesperrt.

Weiterhin gilt: 20 vorbestehende mypy-Fehler, drei Pytest-Warnungen, sieben
Tests mit `@pytest.mark.echter_unterprozess`, und
`test_review_selection_bridge_rejects_untrusted_requests_and_persists_canonical_state`
als flatterhaft — im Weckerlauf nicht geflattert.

---

## 7. Was nicht gebaut ist

**Abschnitt 11 des Bestands vom 27.8. abends gilt mit zwei Streichungen:**
**der Wecker und der Aufraeumer sind gebaut.**

Offen bleiben:

1. **Die Uploadstufe** — und vor ihr der Audit-Test (Uebergabe vom 28.8.,
   Abschnitt 7.4).
2. **Eine Oberflaeche fuer die Shorts-Linie** — bisher nicht erkundet.
3. **Eine Stelle, an der festgehalten wird, welche Shorts tatsaechlich
   veroeffentlicht wurden.** Ohne sie laesst sich die Vorauswahl nicht gegen
   das eigentliche Mass pruefen. Ob es geplant ist, ist **nicht belegt**.

Der naechste Schritt ist keiner davon, sondern der **Betrieb**: die Linie
einige Videos fahren, damit die Zahl entsteht, die Punkt 3 misst (Uebergabe vom
28.8., Abschnitt 6(1)).

---

## Pruefsteine dieses Auftrags

Sie stehen in der Antwort des ausfuehrenden Fensters, nicht hier. Dieses
Dokument, `ORCHESTRATOR-UEBERGABE-2026-08-28.md` und die Berichtigung in
`WECKER.md` sind die drei einzigen Dateien, die der Auftrag `uebergabe-28-08`
angefasst hat.
